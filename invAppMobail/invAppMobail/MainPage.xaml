<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:invAppMobail"
             x:Class="invAppMobail.MainPage">



    <ScrollView>
        <StackLayout>
            <Button 
                x:Name="BarcodeRequest" 
                Text="Искать в базе (скан штрихкода)" 
                Clicked="BarcodeRequest_Click">
            </Button>
            <Button 
                x:Name="NumberRequest" 
                Text="Искать в базе (фильтр)"
                Clicked="NumberRequest_Click">
            </Button>
            <Button 
                x:Name="Inventory" 
                Text="Инвентаризация"
                Clicked="Inventory_Click">
            </Button>
            <Button 
                x:Name="Settings" 
                Text="Настройки"
                Clicked="Settings_Click">
            </Button>
            <Button x:Name="Exit"
                Text="Выход"
                Clicked="Exit_Click"    
                >
            </Button>
            
        </StackLayout>        
    </ScrollView>

</ContentPage>
